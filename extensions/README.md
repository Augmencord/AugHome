# AugHome Extensions

This directory houses built-in, bundled extensions for AugHome IDE.

## Directory Structure

- `git-lens/`: Git blame, inline commit authors, and history visualization.
- Additional language packs, themes, and tool connectors can be placed here as individual workspace packages.
