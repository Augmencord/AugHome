# AugHome Editor (`@aughome/editor`)

React + Monaco Editor frontend interface for AugHome IDE. Renders the workbench canvas, interactive file tabs, diff previewers, inline completions, and the conversational Augagent sidebar panel.
